spring:
  application:
    name: ecommsample
  ai:
    mcp:
      server:
        name: ecommerce-tools        # Name this clearly — it appears in LLM context
        version: 1.0.0
        type: SYNC
  server:
    port: 8081
  logging:
    level:
      org.springframework.ai: INFO
